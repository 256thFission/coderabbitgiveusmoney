# GitHub Personal Access Tokens (comma-separated, no spaces)
# Create tokens at https://github.com/settings/tokens with "read:user" and "repo" scopes
# Using multiple tokens gives you 5,000 req/hr per token (round-robin rotation)
GITHUB_TOKENS=ghp_token1,ghp_token2,ghp_token3

# CodeRabbit API key (Pro plan required)
# Get yours at: https://app.coderabbit.ai → Organization Settings → API Keys
CODERABBIT_API_KEY=cr-xxxxxxxxxx
